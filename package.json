{
  "name": "@dczysz/node-red-bit-encode",
  "version": "1.0.0",
  "description": "Set of nodes to encode multiple integers or booleans into a single integer and decode back to the original key value pairs",
  "main": "index.js",
  "scripts": {
    "test": "jest"
  },
  "keywords": [],
  "author": "dczysz",
  "license": "MIT",
  "node-red": {
    "nodes": {
      "bit-encode": "nodes/bit-encode.js",
      "bit-decode": "nodes/bit-decode.js",
      "bit-config": "nodes/bit-config.js"
    }
  },
  "devDependencies": {
    "@types/jest": "^28.1.8",
    "jest": "^29.0.0"
  }
}
